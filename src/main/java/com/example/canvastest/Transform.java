package com.example.canvastest;

public class Transform {
    // x, y =
    // ax + cy + e, bx + dy + f

    public float a = 1; // Horizontal scaling
    public float b = 0; // Horizontal skewing
    public float c = 0; // Vertical skewing
    public float d = 1; // Vertical scaling

    public float e = 0; // Horizontal translation
    public float f = 0; // Vertical translation
}
